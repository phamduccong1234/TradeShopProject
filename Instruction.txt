* Database folder containing the database for the TradeShop website project.
* The user needs to import the database from this directory through the command prompt with the path to the bin directory 
of the location of the MongoDB installed drive.
* Open the TradeShopProject folder in Visual Studio Code and run the terminal with the command: npm start
